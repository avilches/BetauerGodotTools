using Godot;

namespace Betauer.StateMachine {
    public abstract class BaseStateMachineNode : Node {
        protected static Logger Logger = LoggerFactory.GetLogger<object>(); // TODO WHAAAAAAAAT
    }
}